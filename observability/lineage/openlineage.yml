# Example OpenLineage client config (placeholder)
transport:
  type: http
  url: "https://openlineage.example.com"
  endpoint: "api/v1/lineage"
  auth:
    type: api_key
    api_key: "${OPENLINEAGE_API_KEY}"
namespace: "mini-telecom-etl"
